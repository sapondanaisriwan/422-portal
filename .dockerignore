# Dependencies
node_modules/

# Build outputs (ป้องกันการนำไฟล์ build จากเครื่อง local เข้าไปทับซ้อน)
dist/
build/

# Git
.git/
.gitignore

# Environment variables (เพื่อความปลอดภัย ไม่ควรเอาไฟล์ .env ทะลุเข้าไปใน Image)
.env
.env.*
!.env.example

# Docker-related
Dockerfile
.dockerignore

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Editor & OS files
.vscode/
.idea/
.DS_Store
Thumbs.db